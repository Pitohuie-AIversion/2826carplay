# 极境车库后续开发路线

## 1. 路线定位

第一版 MVP（Phase 0—10）已经完成。后续产品目标从“展示车辆并收集预约意向”升级为：

```text
帮助用户看懂车辆与费用
→ 提交有效预约
→ 收到透明报价
→ 主动确认方案
→ 完成可信交接与履约
```

北极星指标从“预约提交数”调整为“有效确认预约数”。

当前路线不默认开发在线支付、押金冻结、身份证或驾驶证认证、电子合同、自助开锁和自动调度。上述能力必须通过 Phase 17 的独立决策门，并由用户再次明确确认。

## 2. 固定执行规则

1. 严格按照 Phase 11 → Phase 17 的顺序推进。
2. 每轮只实现一个 Phase，完成测试与文档同步后停止，等待用户验收。
3. 继续使用微信小程序原生技术栈和微信云开发，不引入跨端框架或大型 UI 依赖。
4. 优先复用现有页面、组件、云函数、权限、审计、错误日志和隐私机制。
5. 所有公开接口继续使用字段白名单，管理操作继续执行服务端鉴权与审计。
6. 用户端不公开完整车牌、VIN、发动机号、内部备注、管理员身份或其他无关信息。
7. 每个 Phase 都必须补齐自动化测试，并运行 `npm run check:release`。
8. 未完成真实业务配置前，界面不得伪造支付成功、合同生效、信用免押或自动锁车状态。

## 3. 总体优先级

| 阶段 | 主题 | 产品价值 | 复杂度 | 默认状态 |
| --- | --- | --- | --- | --- |
| Phase 11 | 费用与租赁规则透明化 | 高 | 低—中 | 已完成 |
| Phase 12 | 报价与用户确认闭环 | 最高 | 中 | 下一阶段 |
| Phase 13 | 车辆可信档案 | 高 | 中 | 待 Phase 12 |
| Phase 14 | 数字交接与车况留证 | 高 | 中—高 | 待 Phase 13 |
| Phase 15 | 真实档期与价格日历 | 高 | 高 | 待 Phase 14 |
| Phase 16 | 内容增长与渠道归因 | 中 | 中 | 待 Phase 15 |
| Phase 17 | 交易能力决策门 | 决策项 | 高 | 不自动实施 |

---

## Phase 11：费用与租赁规则透明化

状态：已于 2026-08-09 完成，等待用户验收。

### 目标

消除当前“参考价格、最终以顾问确认为准”带来的不确定性，让用户在提交预约前知道费用如何组成、哪些项目包含或不包含、押金与取消规则是什么。

### 范围

- 在车辆公开数据中补充结构化价格摘要，而不是只展示 `priceText`。
- 在车辆详情页展示：
  - 基础日租价与计价单位；
  - 基础保障或保险说明；
  - 可能产生的服务费、取送车费与超时费用；
  - 车辆押金、违章押金及退还规则；
  - 取消与改期规则；
  - 油量或电量规则；
  - “当前展示为预估，正式金额以有效报价单为准”的明确边界。
- 运营后台支持维护通用租赁规则，车辆仅保存必要的差异化配置。
- 补充匿名事件：费用说明查看、租赁规则查看、电话咨询、分享、档期检查结果。
- 分析页增加“详情 → 查看费用 → 开始预约 → 提交预约”漏斗。

### 不做

- 不生成正式报价单。
- 不接入支付、押金、优惠券或动态调价。
- 不声称预约提交后已经锁定车辆。

### 验收标准

- 用户不联系客服也能理解主要费用项目和关键租赁规则。
- 通用规则由运营配置读取，公开接口不返回内部字段。
- 旧车辆缺少新字段时有安全、清晰的降级展示。
- 匿名统计不保存 OpenID、手机号、姓名、备注或其他表单内容。
- `npm run check:release` 全部通过。

---

## Phase 12：报价与用户确认闭环

### 目标

把“客服已联系”升级为可追踪的“顾问发出报价—用户确认或申请调整”，让有效预约成为系统内的明确结果。

### 核心流程

```text
待联系 pending
→ 已沟通 contacted
→ 已报价 quoted
→ 用户已确认 confirmed
→ 已完成 completed

任一允许节点 → 已取消 cancelled
报价节点 → 用户申请调整 adjustment_requested → 重新报价
```

### 范围

- 建立独立的 `booking_quotes` 集合，保留报价版本和审计历史，不覆盖旧报价。
- 报价字段至少包括：
  - 关联预约与车辆；
  - 租期与计费天数；
  - 基础租金；
  - 保障/保险费用；
  - 服务费、取送车费和其他明确费用；
  - 应付预估总额；
  - 押金说明；
  - 报价有效期；
  - 用户可见备注；
  - 版本、创建时间和状态。
- 管理员在现有预约管理详情中创建、修改草稿并发送报价。
- 用户在现有预约详情中查看费用明细、确认报价或申请调整。
- 报价发送、用户确认和报价失效均执行幂等校验、权限校验、审计和错误记录。
- 复用现有订阅消息能力提醒用户查看报价或确认结果；通知失败不得阻断主流程。
- 分析页增加报价发送率、报价确认率、平均确认耗时和调整申请量。

### 不做

- 用户确认不等于付款，不生成支付订单。
- 不上传身份证、驾驶证或合同文件。
- 不自动将咨询冲突视为车辆库存锁定。

### 验收标准

- 报价合计由服务端根据明细校验，客户端不能篡改总额。
- 用户只能访问自己的预约和报价；顾问权限由服务端判断。
- 报价版本可追溯，已发送版本不能静默修改。
- 重复点击不会产生重复确认、重复报价或非法状态跳转。
- 旧预约数据可以正常读取和显示。
- `npm run check:release` 全部通过。

---

## Phase 13：车辆可信档案

### 目标

降低用户对真实车况、保险和经营主体的疑虑，为高价值车辆预约建立可信依据。

### 范围

- 车辆公开档案增加：
  - 实拍或资料更新时间；
  - 最近保养/检查日期及摘要；
  - 当前已知外观情况摘要；
  - 商业保险与救援能力摘要；
  - 车辆年份、座位、能源、变速箱等决策字段；
  - 客服、监督和投诉渠道。
- 运营端维护公开摘要与内部记录，二者使用不同字段白名单。
- 车辆资料过期、缺失或待核验时给出真实状态，不使用虚假的“已认证”。
- 增加可信档案查看与电话/预约转化统计。

### 不做

- 不公开完整保险单、行驶证、VIN、发动机号或完整车牌。
- 不建立用户评分体系，除非已经具备真实履约样本和审核机制。

### 验收标准

- 用户可以判断资料更新时间、车辆基础状况和保障边界。
- 管理员能识别长期未更新档案。
- 公开数据与内部车辆档案严格隔离。
- `npm run check:release` 全部通过。

---

## Phase 14：数字交接与车况留证

### 目标

为已确认预约建立取车、还车双方可核对的车况记录，减少划痕、里程、油量/电量争议。

### 范围

- 建立与已确认预约关联的交接记录。
- 取车与还车分别记录：
  - 拍摄时间；
  - 规定角度的车辆照片；
  - 里程；
  - 油量或电量；
  - 已知损伤和补充说明；
  - 顾问提交与用户确认时间。
- 照片上传沿用现有格式、大小、安全规则和失败清理队列。
- 交接记录提交后版本化保存，修改必须留下审计痕迹。
- 提供一键救援或紧急联系电话，但不伪造实时救援调度。

### 不做

- 用户确认仅表示核对交接内容，不等同于具有电子签章效力的合同。
- 不做自动车损识别、自动扣款或维修定价。

### 验收标准

- 非目标预约用户无法读取交接记录或照片。
- 图片、里程和能源数据校验完整，重复提交幂等。
- 删除与归档遵守现有隐私和存储清理机制。
- `npm run check:release` 全部通过。

---

## Phase 15：真实档期与价格日历

### 目标

在已经具备确认预约与交接流程后，将“同期咨询提醒”升级为可信的可租日历与人工可控的库存占用。

### 范围

- 建立车辆不可用区间、维护区间、人工保留和已确认预约占用。
- 只有已确认预约才能占用档期；普通咨询只提示冲突，不自动锁车。
- 支持运营人员人工释放、延长或调整占用，并记录原因和审计日志。
- 展示按日基础价格和明确的节假日/特殊日期价格规则。
- 用户预约前看到可用日期和价格摘要；最终仍由 Phase 12 报价确认。
- 增加档期不足、价格变化与预约转化统计。

### 不做

- 不做 AI 动态定价。
- 不做多门店自动调车、异地还车结算或分钟级分时租赁。

### 验收标准

- 相同车辆的确认占用不能重叠。
- 档期变更具有事务或等价并发保护。
- 维护、停用和已删除车辆不能产生新占用。
- 时间边界统一按中国标准时间处理。
- `npm run check:release` 全部通过。

---

## Phase 16：内容增长与渠道归因

### 目标

在预约闭环稳定后，通过自驾场景内容和微信分享获取更高质量流量，而不是依赖优惠券或复杂会员体系。

### 范围

- 复用现有内容页承载路线、车型建议、取还车指南、车辆使用提示。
- 支持“车型 + 路线/场景”内容组合，例如周末短途、商务接待、多人出行和新能源体验。
- 优化车辆分享卡片、预约分享入口和分享落地路径。
- 记录不含个人身份的来源场景、内容来源和分享转化。
- 分析不同内容、车辆和来源的有效确认预约率。

### 不做

- 不做积分、会员等级、优惠券中心或社区。
- 不采购地图 SDK，不做实时导航。
- 不在没有真实合作关系时宣称酒店、景区或交通枢纽套餐。

### 验收标准

- 分享落地后可以恢复对应车辆或内容上下文。
- 渠道参数经过白名单和长度校验，不能注入任意跳转地址。
- 分析结果不保存用户身份或敏感行程详情。
- `npm run check:release` 全部通过。

---

## Phase 17：交易能力决策门

### 目标

根据真实运营数据决定是否进入完整租车交易系统开发。本 Phase 只形成评估结论，不自动实现高风险能力。

### 建议进入条件

- 已连续稳定运营至少 4 周；
- 已积累足够的真实确认预约和履约样本，建议不少于 30 单；
- 报价确认、取消、交接、客服与异常处理均有可复核数据；
- 车辆、主体备案、保险、合同、押金和退款规则已经由业务及合规负责人确认；
- 运营团队确实受到人工收款、人工核验或合同处理效率限制。

### 独立评估项

- 微信支付与退款；
- 车辆押金、违章押金及资金管理；
- 身份证和驾驶证查验；
- 电子合同与电子签名；
- 信用免押；
- 发票；
- 自助取还、车联网和远程开锁；
- 异地还车与多门店库存协同。

每一项都必须单独确认业务主体、平台类目、隐私影响、数据保存期限、安全方案、退款与客诉流程，再决定是否建立新的开发 Phase。

## 4. 每阶段统一交付格式

```text
完成阶段：
修改文件：
新增文件：
删除文件：
主要改动：
数据与隐私影响：
测试方式与结果：
发现的问题：
是否可以进入下一阶段：
下一步建议：
```

## 5. 下一步

Phase 11 已完成。用户验收后明确发出“开始 Phase 12”，再实施报价与用户确认闭环；不得在同一轮继续开发后续阶段。
