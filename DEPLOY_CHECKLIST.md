# 极境车库上线部署清单

## 1. 部署前确认

- 当前云环境：`cloud1-d8gtmns36320e045e`
- 云函数根目录：`cloudfunctions/`
- 本地部署目标：`Nodejs20.19`；当前环境中的既有函数仍为受支持的 `Nodejs16.13`。CloudBase 不支持原地切换既有函数运行时，迁移必须安排维护窗口并逐个重建验证，禁止在正常营业期间批量删除重建
- 云函数 SDK：全部固定为 `wx-server-sdk@4.0.2` 并包含锁文件；禁止使用 `^`、`~` 或 `latest` 等浮动版本
- `npm run check:release` 通过，当前为 `95 suites / 626 tests`
- `npm run check:structure` 通过，当前已核对 `26` 个页面与 `49` 个云函数，页面路由、必需文件、云函数目录和部署清单保持一致
- `npm run check:secrets` 通过，仓库未发现私钥、环境文件或硬编码生产凭据
- `npm run check:package` 通过，当前源码主包估算约 `1.34 MiB`，距离 `2 MiB` 保守阻断线约 `0.66 MiB`

## 2. 必须部署的云函数

以下函数已在 `cloudbaserc.json` 中纳入部署清单，部署时应全部同步：

- `analyticsOverview`
- `analyticsTrack`
- `analyticsCleanup`
- `auditLogList`
- `bookingCancel`
- `bookingCalendarList`
- `bookingCreate`
- `bookingDetail`
- `bookingExportCsv`
- `bookingList`
- `bookingMyDetail`
- `bookingMyList`
- `bookingUpdateAdminRemark`
- `bookingUpdateCoordination`
- `bookingUpdateMyContact`
- `bookingUpdateStatus`
- `bootstrapAdmin`
- `errorLogList`
- `favoriteMyList`
- `favoriteSet`
- `favoriteStatus`
- `garageVehicleList`
- `getMyPermissions`
- `getOpenid`
- `logExportCsv`
- `operationConfigGet`
- `operationConfigUpdate`
- `operationSummaryGet`
- `pendingFileDeletionProcess`
- `privacyRequestCreate`
- `privacyRequestDataInventory`
- `privacyRequestCancel`
- `privacyRequestList`
- `privacyRequestMyList`
- `privacyRequestUpdateStatus`
- `roleList`
- `roleUpsert`
- `systemHealthCheck`
- `vehicleAvailabilityCheck`
- `vehicleCreate`
- `vehicleDelete`
- `vehicleDetail`
- `vehicleImageUpdate`
- `vehicleList`
- `vehiclePublicDetail`
- `vehicleRestore`
- `vehicleRetire`
- `vehicleUpdate`
- `vehicleUpdateStatus`

## 3. 数据库集合检查

上线前确认以下集合已存在：

- `analytics_events`
- `vehicles`
- `bookings`
- `roles`
- `app_configs`
- `audit_logs`
- `error_logs`
- `favorites`
- `pending_file_deletions`
- `privacy_requests`

## 4. 建议索引

建议在云数据库控制台补以下索引：

- `analytics_events.createdAt`
- `audit_logs.createdAt`
- `audit_logs.action`
- `error_logs.createdAt`
- `error_logs.function`
- `favorites.openid + createdAt`
- `favorites.openid + vehicleId`
- `bookings.openid + createdAt`
- `bookings.createdAt`
- `bookings.vehicleId`
- `bookings.status + createdAt`
- `bookings.status + coordinationStatus`
- `bookings.startDate + endDate`
- `privacy_requests.openid + createdAt`
- `privacy_requests.createdAt`
- `roles.openid`（建议唯一索引，防止并发分配权限时产生重复角色记录）
- `vehicles.plateNumber`（必须为唯一索引；创建前先清理历史重复车牌和缺失车牌的记录）
- `vehicles.updatedAt`

其中 `bookings.openid + createdAt` 仍建议创建，用于提升“我的预约”和预约防刷查询性能。`bookingMyList` 与 `bookingCreate` 已提供缺少复合索引时的分批查询与云函数内排序兜底，但兜底路径会增加数据库读取次数，不应替代正式索引。

## 5. 环境变量

- `bootstrapAdmin` 需要配置 `BOOTSTRAP_TOKEN`
- `BOOTSTRAP_TOKEN` 必须使用 32 至 256 位随机口令；过短、过长或未配置时初始化保持关闭
- 未配置时，首个管理员初始化会被锁定
- 首个管理员写入使用数据库事务和固定初始化记录抢占，并发请求只能有一个账号成功；事务能力不可用时保持关闭
- 初始化前只按当前 OpenID 读取角色标记，并只读取 `_id` 判断权限集合是否已有记录；不得扫描或返回其他权限账号的 OpenID 与审计字段
- 初始化入口仅在开发版或体验版显示，口令只允许配置在云函数环境变量中，不得写入前端代码、`cloudbaserc.json` 或仓库
- 预约状态订阅模板可在“运营配置”中填写；如设置云函数环境变量 `BOOKING_STATUS_TEMPLATE_ID`，该值优先于页面配置
- `BOOKING_NOTIFY_STATE` 可选值为 `developer`、`trial`、`formal`，未配置时默认发送到正式版

### 预约状态订阅模板

- 在微信公众平台申请一次性订阅消息模板，并把模板 ID 填入“运营配置 → 预约状态订阅模板 ID”
- 模板字段需按以下口径配置：车辆名称 `thing1`、预约状态 `phrase2`、服务说明 `thing3`、更新时间 `time4`
- 用户提交预约时会自行选择是否接收；拒绝或关闭订阅不会影响预约提交
- 用户每次授权通常只对应一次消息发送机会，应在需要继续提醒时引导用户再次授权
- 用户未订阅属于正常业务结果，不写入错误日志；配置读取、接口缺失或真实发送异常会写入脱敏错误日志

## 6. 安全与隐私配置

- 按 `security-rules/manifest.json` 覆盖全部 10 个集合，并为每个集合应用 `security-rules/database-deny-client.json`；数据库统一通过云函数访问
- 云存储应用 `security-rules/storage.json`：仅公开读取 `vehicle-images/`，仅允许已登录、非匿名的文件创建者写入自身车辆图片，扩展名只允许 JPG、JPEG、PNG、WebP，单文件不超过 10 MB；车辆详情管理页应在上传前执行相同的格式与大小预检
- 云函数权限控制应用 `security-rules/functions.json`，用受支持的 `auth != null` 拒绝未登录调用；管理员等细分权限和敏感操作身份继续由云函数根据微信 `OPENID` 在内部校验
- 安全规则不会随普通云函数上传自动生效；必须在云开发控制台手工配置，等待 1–3 分钟后再真机复核
- `npm test` 中的安全规则测试必须通过，确认集合清单完整且小程序业务代码没有直接访问数据库
- `project.config.json` 必须继续排除 `__tests__`、`security-rules`、`node_modules` 和项目文档，防止开发文件占用正式小程序包容量
- `data/cars.js` 与 `assets/cars/` 仅作为早期本地示例保留，当前运行时不得引用，并继续排除出正式上传包
- 上传或部署前执行 `npm run check:secrets`；发现 `.env`、私钥文件或硬编码生产凭据时禁止继续发布
- 联网执行 `npm run audit:cloud`，出现严重级漏洞时禁止部署；当前 `wx-server-sdk@4.0.2` 已消除旧版本的严重漏洞，但 npm 仍报告 5 项高危和 1 项中危上游依赖问题，应持续跟进官方 SDK 更新
- 上传前执行 `npm run check:package`；达到 `1.75 MiB` 时先压缩资源或规划分包，达到 `2 MiB` 时禁止继续上传
- `project.config.json` 与 `project.private.config.json` 的合法域名校验都必须保持开启；开发环境不得依赖关闭校验才能正常运行
- `sitemap.json` 只允许索引首页车库、公开车辆详情和公开内容页，预约、我的、个人记录和管理页面不得进入微信搜索
- 发布清洁测试必须确认业务源码中没有 `console.log`、`console.debug`、`console.info` 或 `debugger` 残留，并确认小程序与云部署配置使用同一环境
- 微信公众平台“用户隐私保护指引”覆盖姓名、手机号、日期、城市、备注和 OpenID
- 完成小程序备案、主体认证和匹配的服务类目
- 配置在线客服，并确认客服电话有效
- 确认隐私政策中的个人信息查询、更正和删除渠道可实际执行
- 用户可从“我的 → 个人信息申请”提交查询、更正或删除申请并查看处理反馈
- 相关数据核验只允许管理员访问；核验审计只记录申请编号、类型和分类数量，不记录目标用户 OpenID、姓名、手机号或申请正文
- 异常日志不得保存整段用户请求；图片删除失败日志只记录文件数量，完整文件 ID 仅保留在受限的待清理队列中
- 图片删除失败的运行日志与队列上下文只记录车辆 ID、动作和错误码，不记录 OpenID；原始云文件地址仅允许保留在受限待清理队列的 `fileList`
- 图片已上传但车辆资料入库结果不明确时，页面必须调用管理员受限的 `cleanupUpload` 服务端动作；每张图片使用由车辆 ID 和文件 ID 不可逆生成的确定性任务编号，并按文件上传时间固定最早核验时点，重复提交不得产生重复记录或无限延后任务；处理前核对车辆当前图片引用，只删除未被引用的孤儿文件
- 车辆新增或编辑失败时，错误日志与云函数控制台不得记录完整车牌；权限保存失败时不得记录目标用户 OpenID
- `analytics_events` 只保存事件类型、车辆 ID 和时间，不得写入 OpenID、姓名、手机号、城市、备注或页面输入
- “查询 OpenID”工具只返回当前用户的 OpenID；不得向前端附带 UnionID、AppID 或云环境标识，身份缺失时必须拒绝返回成功结果
- 管理员可在“数据分析 → 匿名数据保留”中分批清理超过 90 天的匿名行为事件，避免集合无限增长

## 7. 上线前人工检查

### 用户侧

- 首页车辆列表正常加载，失败时显示明确错误图标，重试与加载更多入口使用对应原生图标，列表结束提供完成反馈
- 首页可在当前分类内切换“全部状态 / 可预约”；横向车型分类在小屏设备上应显示右侧渐隐提示，切换后当前分类自动回到可见区且末项不被遮挡；搜索无结果、没有可预约车辆与空车库显示对应原生状态图标，并可继续搜索、清除搜索或一键恢复全部状态；车辆卡片不得向客户显示“闲置、在用”等后台口径
- 车辆详情页正常打开，失败或车辆不存在时显示带品牌标识的明确空状态，并使用原生重试或返回图标
- 后台修改客服电话后，首页、车辆详情、公开内容页和“我的”页应统一使用最新号码；配置读取失败时保留默认号码且不影响车辆浏览
- 车辆详情与首页统一显示“可预约、使用中、维护中、已预约”，并根据状态提供立即预约、咨询候补、咨询档期或咨询恢复时间
- 预约表单随姓名、手机号、取还车日期和隐私同意状态实时更新完成度及下一项提示，并以“联系资料—出行日期—隐私确认”三段状态区分当前、已完成和待完成步骤；姓名、手机、取还车日期和隐私确认应统一标记必填，取车城市明确标记选填，已完成字段显示轻量蓝色描边；隐私同意区应扩大勾选与政策链接触控面积，勾选后显示蓝色完成描边并同步辅助状态；资料完整前提交按钮保持不可用，提交处理中显示原生加载圈、“正在提交”文案并隐藏业务图标和箭头；填写、成功、加载及失败状态均应避开左右和底部安全区，页面不得把这些敏感表单内容写入本地存储
- 预约日期、取车城市以及车辆新增和编辑表单的所有选择控件使用原生下拉箭头，并提供与字段一致的辅助名称，不使用字符箭头占位；新增与编辑车辆应按车牌号、车辆类型、品牌型号、注册日期和使用状态实时显示完成度及下一项提示，五项控件声明必填并在完成后显示轻量蓝色描边，车辆身份、展示租赁和内部档案使用对应原生图标，底部吸底提交栏在五项完整后显示可提交状态；创建、保存、图片管理、失败重试与返回入口均使用对应业务图标
- 预约日期、取车城市及车辆新增和编辑表单中的所有选择器应具备一致的压暗、描边和轻微缩放反馈
- 车辆新增页的图片上传说明使用原生信息图标，不使用字母 `i` 作为图标
- 车辆详情管理应区分状态操作、基础档案、管理记录、公开说明和内部备注，公开与内部内容显示可见范围；停用操作位于独立危险区，图片上传、封面设置、移除和空相册使用对应原生图标，返回列表与编辑车辆保持吸底可达
- 车辆管理列表应分开展示封面摘要、核心参数、档案记录、状态切换和维护操作；根据封面与图片数量显示待补封面、基础素材或素材充足进度，空封面、详情、编辑、停用、恢复、删除、查询、重置和加载更多使用对应原生图标；近 7 天记录应使用状态标签、线条箭头、整行按压反馈和明确辅助名称进入详情，暂无新增时显示车辆图标与后续说明；列表完成与读取上限提供明确反馈，停用与删除不得和主要编辑操作混在同一层级
- 管理端封面或图片云文件加载失败时应显示暖色异常占位；异常图片不得继续显示“设为封面”，但必须保留移除入口以便清理失效记录
- 预约页正常提交
- 配置订阅模板后，提交预约时会出现微信订阅授权；接受或拒绝均能继续提交
- 待联系或已联系预约的详情页可再次订阅下一次状态提醒，避免一次性授权在首条通知后耗尽
- 联系资料、有效取还车日期或隐私确认任一未完成时不能提交预约
- 相同车辆和日期不能重复预约，频繁提交会被限制
- 用户选完取还车日期后可看到同期咨询提示，档期宽松、存在同期咨询和等待确认分别使用原生成功、警告和待确认图标；提示不包含其他用户信息，查询失败或存在同期咨询均不阻止提交
- 表单信息和隐私同意完成后显示提交确认摘要，手机号必须脱敏，日期跨度不得用于暗示最终计费天数；提交按钮使用原生前进箭头并具备按压反馈
- 预约提交成功后应进入带原生勾选图标的结果页，展示等待顾问联系状态，并可直达本次预约详情或返回车库
- 弱网重试或重复点击不会为同一次提交创建多条预约
- 公开首页和详情页不显示完整车牌
- 预约创建只读取车辆名称、车型、车牌和状态；预约车辆名优先使用品牌车型，车牌仅允许脱敏兜底
- 公开首页和详情页只显示“公开说明”，车辆内部备注、VIN 和发动机号不得出现在公开接口
- 车辆详情图片可从当前图片打开全屏预览，多图时显示分段进度与当前位置，单图不应循环；放大预览与收藏按钮使用清晰原生图标；收藏处理中应隐藏心形避免与原生加载圈叠加，并禁用重复点击；底部预约行动栏应与车辆当前状态保持一致，预约与电话咨询入口使用对应业务图标
- 公开首页和详情云函数必须使用数据库字段白名单，车辆内部备注、VIN 和发动机号不得在查询阶段读入公开接口
- “我的收藏”车辆卡片同样使用数据库字段白名单，不得在查询阶段读取 VIN、发动机号或内部备注
- 首页与收藏页复用的 `car-card` 组件必须在组件自身样式中定义可预约、使用中、维护中和已预约状态色，并使用原生详情箭头、整卡按压反馈和明确辅助名称，不依赖全局样式穿透
- `car-card` 缺少封面或等待图片加载时应显示极境车库徽标、品牌档案说明及环形加载反馈；本地徽标不得触发额外网络请求
- 后台车辆列表待上传封面与图片库加载失败状态应使用极境车库徽标，并以蓝灰、暖色样式清晰区分；车辆列表的图片由整张封面入口提供辅助说明，不应重复朗读
- 首页、车辆卡片、加载与空状态、用户页及管理页头复用的极境车库徽标均应声明为装饰图形，相邻文字已经表达内容时不得重复朗读图片
- 首页搜索可匹配车型、品牌和车辆标签，并能与分类、可预约状态组合使用；零结果时可清除搜索或继续加载下一页
- 首页总车源与可预约数量只在车库摘要中重点展示，状态筛选使用紧凑工具条和原生调节器图标，不应重复堆叠同组统计数字
- 首页和待协调工作台的搜索栏在存在关键词时显示圆形关闭图标，具备明确清空语义与按压反馈，不使用“清除”文字挤占输入宽度
- 首页车辆列表支持分页加载，超过 100 辆时仍可继续查看
- 车库、收藏、预约和我的四个核心页面底部导航图标清晰、当前态和按压反馈正确，可互相切换且不会重复叠加当前页面
- 全面屏设备上车库、预约、收藏和我的正文，以及核心导航与车辆详情悬浮操作栏均应避开左右和底部安全区；四个核心页面和车辆详情的最后一项内容不得被吸底区域遮挡；核心导航应提供标签组、标签及当前选中语义，选中项文字层级明确，车库徽标不得重复朗读
- 车辆详情轮播在加载中、图片失败和暂无图片时应显示极境车库徽标与一致品牌档案布局，不得退回空心转圈或纯文字占位；动态车辆封面和相册应使用懒加载、淡入或自定义加载、等比裁切及失败兜底，品牌徽标保持 384×384 且不超过 100 KB
- 所有由视图、图片或文字实现的自定义点击区域均应具备按压反馈与按钮或链接语义；刷新、档期重试、隐私政策、号码复制、备注编辑、车辆封面和图片预览入口还应提供明确辅助名称；同步中或无效状态应标记为不可操作且不显示按压反馈
- 搜索清除、档期重试、刷新数据、重置视图、隐私政策与分区跳转等高频小操作必须保留至少 `56rpx` 有效点击热区，不能只依赖图标或文字自身尺寸
- 状态选择、工作台操作、冲突处理、收藏取消、图片管理、编号复制与失败页等自定义按钮应具备一致按压反馈；加载或禁用状态不得触发缩放
- 价格、日期、里程、预约数量和运营指标应使用等宽数字，数据刷新前后不应出现明显横向跳动
- 预约填写与状态、车辆资料、运营同步、数据核验、隐私处理和上线检查的关键进度组件应集中提供当前数值与状态说明，纯图形轨道不得重复朗读；动态宽度进度条应使用一致的平滑过渡
- 全站界面样式中的文字字号不得低于 `14rpx`；日历数量、复制提示、进度眉题、服务数量、上线统计和日志类型等关键微文案应保持清晰可辨
- 日历数量、复制提示、进度眉题、服务数量、上线统计和日志类型等关键微文案在深色卡片上的文字对比度不得低于 `4.5:1`
- 带原生加载圈的导出、保存、订阅、取消、处理和分页按钮应在处理中隐藏业务图标，并把静态动作文案切换为明确的“正在…”状态
- 我的预约支持分页加载
- 我的预约列表与详情数据库查询必须使用用户可见字段白名单；详情可读取 OpenID 仅用于服务端归属校验，不得读取或返回内部备注、协调状态和协调说明
- 我的预约列表按当前已加载记录汇总进行中、已完成和已取消数量；仍有下一页时不得把局部汇总表述为全部数据
- 我的预约状态筛选应收纳在状态摘要卡内；筛选按钮不重复显示摘要数字，但辅助名称必须保留对应状态数量
- 我的预约列表可按全部、进行中、已完成和已取消筛选；筛选无结果时应展示调节器空状态图标并能恢复查看全部预约
- 我的预约列表与详情应按待联系、已联系、已完成和已取消状态展示一致的当前阶段说明及下一步建议；列表卡片左侧强调色应跟随状态变化，序号在第 10 条后不得出现多余前导零，并使用“已提交—已联系—已结束”三阶段轨迹；状态汇总、车辆、城市、备注与查看、取消等高频操作显示原生图标；取还车日期路线统一使用原生箭头，分页加载期间入口必须禁用以避免重复请求
- 我的预约详情可正常查看；短预约编号与复制入口应组成清晰凭证操作并提供包含编号的辅助名称，正文在全面屏设备上避开左右和底部安全区
- 预约详情应显示便于核对的短预约编号、原生状态节点并支持复制完整编号；复制、编辑、订阅、查看车辆、全部预约、取消、重试与返回入口均使用对应业务图标且功能正常
- “我的”页常用服务区使用日历、收藏和隐私锁原生图标，可分别直达我的预约、我的收藏和个人信息申请；身份卡应提供姓名、身份与说明的完整分组语义，品牌图形不得重复朗读；权限同步期间更多服务区显示四行局部菜单骨架，完成后显示中文服务数量，按实际功能显示原生图标而非单字徽章，待办徽标数量应包含在入口辅助名称中；快捷卡、运营概览和更多服务统一使用线条方向箭头；客服卡显示专属顾问标识，在线客服与电话咨询使用独立业务图标；后台入口仍应按当前账号权限显示
- 常见问题、平台规则和隐私政策可通过具备标签组、标签和当前选中语义的服务指南切换，章节数量使用中文文档徽标；章节目录应显示横滑提示、自动保持当前项可见并能定位到对应正文，页面正文避开左右和底部安全区；隐私权益和客服支持分别使用资料确认与耳机原生图标，提交个人信息申请、在线客服与电话咨询入口使用独立业务图标及统一按压反馈，不使用单字或问号徽章
- 车辆详情可收藏和取消收藏，“我的收藏”只展示当前 OpenID 的记录，首次载入使用包含车型、状态、标签、价格和操作区的等高车辆卡片骨架；全部与可预约筛选应直接显示对应数量并提供选中与朗读反馈；取消收藏前会二次确认，取消、重试、查看全部、加载更多、浏览车库和列表完成反馈使用对应原生图标
- 收藏写入和档期查询只读取车辆状态，匿名分析车型补全只读取品牌车型
- 收藏列表不返回完整车牌、VIN、发动机号或内部备注，停用和已删除车辆不再展示
- 收藏列表查询收藏记录时只读取 `_id`、`vehicleId` 和 `createdAt`，收藏状态检查只读取 `_id`，不得加载完整收藏记录或未来新增的用户字段
- 待联系或已联系的预约可以修改姓名、手机号、城市和备注，不能借此修改车辆或日期
- 用户不能修改他人的预约，已完成或已取消的预约不能修改联系信息
- 用户可取消自己的预约
- 同类型隐私申请处于待处理或处理中时不可重复提交，提交频率过高会被限制
- 个人信息申请说明达到最低要求后才能提交；页面顶部展示统一圆形品牌徽记及当前账号隐私提示，正文避开左右和底部安全区；查询、更正和删除类型使用对应原生图标并具备单选语义，类型与申请说明明确标记必选/必填，说明输入聚焦或达到最低要求时显示对应描边反馈；用户端首次载入申请记录时显示与真实记录结构一致的卡片骨架，加载失败、筛选无结果和暂无记录分别显示告警、筛选和文档图标，记录可按处理中与已结束筛选，筛选控件提供分组、选中状态及对应数量辅助名称，并显示对应阶段说明；提交、撤回、重试、查看全部、加载更多和列表完成反馈使用对应原生图标
- 用户可撤回自己的待处理隐私申请；管理员已开始处理后不能撤回，并发操作不会互相覆盖
- 隐私申请重复检查、用户撤回和管理员状态更新的前置读取必须使用用途字段白名单，不得加载申请正文、处理说明、处理人或无关时间字段
- 用户端隐私申请列表只能查询当前 OpenID 的记录，且不返回处理管理员身份

### 管理侧

- “我的”页能按权限显示管理入口
- 运营概览、数据分析、预约日历、待协调工作台、权限、配置、审计与错误日志、上线检查、隐私申请及数据清单、预约与车辆管理详情等管理页面，以及车辆新增/编辑共享表单，均应避开左右和底部安全区；普通竖屏仍保持统一基础留白
- “我的”页在个人卡片下汇总预约协调、隐私申请和存储清理待办，三类摘要使用日历、隐私锁和存储箱原生图标，同步中、部分同步和已同步使用对应状态标识，并可直接进入运营概览；菜单入口继续显示各自待办数量，单个集合不可用时会标记部分同步且不影响其他数字
- 有管理权限时，“运营脉搏”应紧随个人身份卡并优先于个人常用服务展示，使用明确侧边强调；辅助名称包含当前待处理总数
- “运营概览”按当前账号权限汇总预约、车辆和隐私待办，首次载入显示运营脉搏、行动队列和指标卡结构化骨架，完成后展示数据源同步进度、运营状态、行动项总量和关注模块；预约、车辆、隐私、存储待办以及统计与近期记录使用对应原生图标，近期记录和分区导航提供按压反馈、明确辅助名称与线条箭头，较长的动态标题最多展示两行；单项加载失败时其余数据仍可查看，对应统计区显示业务图标与刷新建议
- 预约管理角标继续统计待联系数量；待协调工作台角标和运营提醒统计待联系或已联系且尚未完成协调的数量
- “数据分析”可切换近 7/30 天，首次载入显示指标、漏斗和趋势结构化骨架；指标卡使用对应原生图标，预约漏斗显示阶段编号和相对进入车库的转化率，每日趋势带辅助网格并突出周期峰值，30 天视图显示渐隐横滑提示且末项不被遮挡，热门车型前三名使用金银铜层级徽章，较长的车型名最多展示两行；暂无车辆行为数据、加载失败、统计上限、匿名隐私说明和 90 天数据清理使用对应原生反馈图标，普通用户不可访问
- “预约日历”只允许预约管理员访问；日期格提供按压反馈、选中及禁用语义，今天使用蓝色短线强调，图例明确区分今天、已选、有预约、待联系与同车重叠，日期辅助名称包含今日、选中、预约数、待联系和冲突状态；离开当前月后应显示带目标图标的“本月”入口，点击后清除旧选中日期并重新查询本月；月度预约、待联系和涉及车辆汇总使用对应原生图标，单日日程以车辆、详情、日期路线和状态图标呈现，整行提供按压反馈和明确辅助名称，日期范围不得使用字符箭头占位；月份切换与预约详情入口使用原生箭头，冲突、空日程、加载失败和使用说明显示对应原生反馈图标；跨天预约覆盖整个日期范围，已取消预约不占用档期
- “上线检查”只允许管理员访问，并以只读方式检查 10 个关键集合、运营配置、订阅模板，以及车牌号和权限账号的缺失/重复情况；检查只返回汇总数量，不返回车牌或 OpenID，也不会新增、修改或删除业务数据
- 上线检查探测集合时只读取记录 ID，车牌和权限账号质量检查只读取对应字段，运营配置只读取模板判断所需的配置值，不得加载首条完整业务记录
- 上线检查页面首次载入应显示总状态、完成度和检查条目结构化骨架，完成后按“可上线、需关注、被阻断”展示完成度，并拆分自动正常、待确认、需修复与人工确认数量；自动检查按数据集合、容量、数据质量和运营配置显示分类图标及状态图标，不使用 `✓`、`!` 或圆点字符代替；六项人工核验保留待确认序号和对应业务图标，完成后显示原生勾选图标并具备复选语义，进度仅保存在当前设备当天，次日自动失效
- 上线检查中的集合缺失属于阻塞项；默认运营配置和未配置订阅模板属于提醒项，不影响基础预约提交
- 车牌号或权限账号存在缺失、重复时属于阻塞项，应先清理历史数据再创建对应唯一索引；单类超过 2000 条时自动检查只做限量扫描，仍须在云控制台全量核验
- 上线检查会对车辆、预约、审计日志、错误日志、隐私申请和匿名分析执行记录规模预警；达到对应功能读取上限约 90% 时提醒归档、清理或升级分页
- 自动检查无法读取云存储实际容量和套餐配额，仍需在云开发控制台核对用量、配额与告警
- 数据库索引、安全规则、微信备案、审核状态与真机流程无法由云函数自动确认，仍须按本清单人工复核
- 日历切换月份时按月从云端重新查询，数据库查询阶段只读取预约编号、车辆、日期和状态；按月索引缺失时的降级读取也不得加载手机号、OpenID、姓名、城市及备注
- 建议创建 `bookings.startDate + endDate` 复合索引；缺少索引时会降级扫描最多 2000 条预约并在云端过滤
- 同一车辆的预约日期发生重叠时，月视图、当日明细和具体预约卡片会显示冲突提示，便于管理员直接定位并人工核对
- 预约详情会列出同车重叠记录，主预约与冲突预约日期范围均使用原生路线组件；拨号、复制手机号、复制 OpenID、打开冲突预约、保存备注、推进状态、取消、重试与返回使用对应业务图标；冲突查询失败、跳过与扫描截断显示明确反馈图标，且查询失败不会影响主预约处理
- 管理员预约列表、预约详情和同车冲突扫描必须分别使用数据库字段白名单；冲突扫描只读取车辆、日期、状态与必要联系信息，不读取 OpenID、用户备注、管理员备注或订阅授权字段
- 取消预约、修改本人联系方式、后台更新状态、保存内部备注和调整协调安排时，更新前读取必须使用用途字段白名单，不得为归属或状态校验加载整条预约记录
- 预约提交的防重复、频率限制与幂等校验必须使用用途字段白名单，不得读取联系方式、用户备注、管理员备注或协调字段
- 车辆新增、编辑、状态切换、图片、停用、恢复和删除的前置读取必须使用用途字段白名单；重复车牌与预约存在性检查只读取记录 ID
- 车辆管理列表与管理详情必须分别使用显式字段白名单；列表不得读取或返回 VIN、发动机号、内部备注、公开长说明和完整图片数组，仅返回封面及图片数量
- 预约管理员可在详情页设置优先、常规或候补，并记录待协调、协调中、已协调状态；已结束预约不可再修改
- 预约管理可组合筛选业务状态、优先级和协调进度；分页与 CSV 导出保持相同筛选条件
- 内容导航、我的预约、我的收藏、隐私记录、首页分类与可预约状态，以及数据分析、车辆、预约、隐私申请、审计日志和错误日志管理页的筛选控件，应具备一致按压反馈、按钮语义与当前选中状态；全站横向内容应启用增强滚动、隐藏原生滚动条并显示渐隐提示，车辆、隐私申请、审计日志、错误日志和待协调工作台的长筛选行切换后当前项应自动回到可见区且末项不被遮挡
- 预约、车辆、审计、隐私和运营统计中的缺省值应使用单个排版破折号或明确中文状态，不得显示 `--`、`N/A`、`null` 等技术占位
- 同步、收藏、保存、更新、上传、设置和处理等进行中动作应统一使用中文省略号 `…`；静态业务状态可保留“处理中”，错误摘要截断不得使用三个英文点
- 所有需要用户确认的原生弹窗均应设置明确的确认按钮动词和颜色；普通更新、恢复、导出与初始化使用品牌蓝 `#528fff`，删除、取消、撤回、停用、驳回、移除与清理使用危险红 `#d46868`
- 原生操作菜单应使用品牌蓝 `#528fff` 并提供顶部任务说明；预约优先级选择直接生效，隐私申请驳回仍须进入危险红二次确认
- 所有原生轻提示均须显式声明图标；失败、警告和部分成功使用 `none`，完整成功可使用 `success`；部分图片被跳过时应显示已上传与跳过数量，不得配成功勾
- 原生轻提示的固定标题不得超过 10 个字符，并应使用用户可理解的业务短句；网络、云函数、文件系统与微信接口失败时不得把 `errMsg`、`error.message` 或其他技术错误原文直接显示给用户
- 后端动态消息进入轻提示前必须统一限制长度：10 个字符以内可保留业务原文，超出时使用当前操作的短回退文案；客服电话等必须完整呈现的长内容应使用信息弹窗，不得截断关键号码
- 所有 `showCancel: false` 的纯信息弹窗均须使用“知道了”作为确认按钮，并统一使用品牌蓝 `#528fff`；结果、提醒失败、导出截断、初始化说明、账号兜底和拨号失败不得退回系统默认“确定”样式
- 所有 `wx.showLoading` 系统加载层均须设置 `mask: true` 并使用带中文省略号的进行中文案，保存、更新、停用、恢复、上传或删除期间不得允许点击底层操作
- 所有 `wx.setClipboardData` 复制入口均须提供成功与失败回调，并在成功回调中使用自定义业务轻提示覆盖微信默认复制 Toast；不得传入微信接口不支持的 `showToast` 参数，OpenID 复制失败时应保留可手动查看内容的兜底
- 所有 `wx.makePhoneCall` 拨号入口均须先校验号码；拨号界面本身作为成功反馈，不得额外显示“已打开拨号”，用户主动取消时保持静默，仅在真实调用失败时提示复制号码或客服电话兜底
- 所有 `wx.navigateTo`、`wx.redirectTo`、`wx.reLaunch`、`wx.navigateBack` 页面导航与 `wx.previewImage` 图片预览调用均须提供失败处理；返回链路应按返回、替代跳转和目标页重启逐级兜底，最终失败时显示明确中文轻提示，不得出现点击后无反馈
- 车辆图片选择与 CSV 文件分享必须区分用户主动取消和真实失败；取消选图或分享时保持当前页面且不显示错误，选择能力真实失败时提示重试，分享能力受限或异常时才打开文件作为兜底
- 自定义按钮不得显示微信原生伪边框；输入光标使用品牌色，车辆新增与编辑字段聚焦时应有清晰且一致的蓝色描边反馈
- 全站输入框与多行文本控件必须提供明确字段辅助名称和占位引导；单行输入必须设置长度边界与有效键盘完成键，手机号应调用纯数字键盘，搜索框应显示搜索键；多行文本必须设置字数上限与实时计数，运营配置字段应提供简短示例或填写说明
- 预约管理卡片以待联系、协调中、已完成三阶段轨迹展示跟进位置和下一步提示；客户、车辆、日期等资料分区清晰，日期范围使用原生路线组件，不使用字符箭头占位；查询、重置、导出、分享、打开、删除、加载更多以及拨号、详情、保存与状态操作使用原生图标；近 7 天记录应使用状态标签、线条箭头、整行按压反馈和明确辅助名称进入详情，暂无新增时显示日历图标与后续说明；导出成功后显示带文件图标的状态卡，取消预约独立显示为危险操作
- 车辆、预约、隐私申请、审计日志和错误日志管理页的搜索框统一使用原生放大镜及清除图标；关键词存在时才显示清除按钮，清除后立即恢复完整列表
- 26 个页面均应存在独立页面配置；服务指南及权限管理、运营配置、审计日志和错误日志的系统导航标题应与当前功能一致，不得短暂继承首页“极境车库”标题；凡实现 `onPullDownRefresh` 的页面必须启用原生下拉刷新，并在深色背景上使用浅色加载指示
- 24 个需要远程首屏数据的页面应使用与真实内容结构对应的加载骨架，骨架容器必须声明 `aria-hidden="true"`，不得让辅助功能逐层读取无文字意义的占位节点；无远程首屏等待的服务指南与新建车辆页无需制造额外骨架
- “待协调工作台”只允许预约管理员访问，首次载入显示四项汇总、协调阶段和处理队列结构化骨架，完成后集中展示待启动、协调中、优先、候补及超过 24 小时仍待协调的预约；全部、优先、超时和候补汇总必须随当前筛选切换蓝、橙、红、灰高亮，汇总、阶段、筛选和排序均提供按压反馈、按钮语义及选中状态；四类汇总、车辆、联系人、号码复制和日期范围显示对应原生图标，日期范围使用原生路线组件，局部同步失败显示原生告警图标且不遮挡可用数据；备注新增/编辑/保存、协调推进、拨号、快速调级、进入处理和跳转预约管理使用统一业务图标，不使用字符加号或箭头占位；只有待联系状态显示原生操作箭头和按钮语义，协调流程完成阶段与暂无待办使用原生勾选图标，重置视图使用恢复图标并具备按钮语义，搜索无结果显示放大镜图标
- 工作台显示最近一次成功同步时间并提供手动刷新；刷新失败时保留上次成功时间，加载或保存期间不重复请求
- 工作台可按待启动或协调中阶段单独查看队列；已协调记录仍会自动移出待办
- 工作台可按车辆、客户、手机号、城市、预约编号或内部备注本地组合搜索；搜索关键词不上传云端
- 启用搜索、分类或非默认排序后会显示“重置视图”，应能一次恢复全部待办和智能排序
- 工作台仅向已鉴权的预约管理员展示手机号并支持复制；缺失或格式异常号码会标红且不能进入剪贴板
- 缺失或格式异常号码可通过“号码异常”单独筛选，并会在智能排序中适度提前，避免不可联系预约长期遗漏
- 工作台支持智能优先、等待最久和用车日期排序；切换排序不增加云端请求
- 今天、明天、7 天内用车及用车日期已过的待办会显示提醒，可通过“用车提醒”单独筛选，并纳入智能排序
- 工作台卡片可直接调整优先、常规或候补级别，保存后自动刷新重排；相同级别不会重复写入
- 工作台卡片可查看、添加、修改或清空 200 字以内的内部备注；备注只在预约管理端显示，保存操作继续写入审计日志
- 待联系预约可从工作台二次确认后标记为已联系；如已配置订阅模板，应分别验证提醒已发送、用户未订阅和发送失败反馈
- 工作台拨号不会自动修改状态；可一键开始协调，完成协调前需二次确认，成功后会刷新队列且已协调记录不再出现在待办中
- 日历仅作人工排期参考，不会自动锁定车辆或改变现有预约状态
- 相同账号、同类匿名行为在 5 秒内由页面和服务端双重去重，服务端使用不可逆的实例级摘要且不落库 OpenID；上报失败可立即重试，每日趋势按中国标准时间的自然日统计
- 匿名行为超过单批读取上限时会继续分批汇总，达到 5000 条统计上限后页面会使用明确警告图标提示
- 匿名分析概览查询只允许读取 `eventType`、`vehicleId` 和 `createdAt`，清理任务只允许读取 `_id`，不得加载完整事件记录或未来新增的设备上下文字段
- 仅管理员可清理 90 天前的匿名行为事件，每次最多 100 条；清理数量写入审计日志，部分失败写入脱敏错误日志
- 权限管理页只允许管理员访问；车辆与预约权限使用带原生图标、说明和选中勾的功能卡，成员列表使用管理员盾牌与成员头像图标，不使用字母头像或 `Admin` 文字徽章；保存期间保存与清空入口均禁用并显示明确文案，保存、清空、编辑、加载更多和列表完成反馈使用对应原生图标
- `getMyPermissions`、`roleList` 和 `roleUpsert` 的角色查询必须使用用途字段白名单，不读取 `createdByOpenid`、`updatedByOpenid` 等内部审计字段；权限接口不得返回当前用户 OpenID
- 所有 32 个按当前 OpenID 查询 `roles` 的后台鉴权入口必须使用 `AUTH_ROLE_FIELDS`，只读取角色、权限和管理员标记；`roleQueryPrivacy` 全仓测试不得被绕过
- 同一 OpenID 的重复权限记录在再次保存时会被统一修正，新增权限记录可抵御并发重复写入
- 存在预约历史的车辆不能物理删除，只能停用；无预约历史的误录车辆仍可删除
- 预约状态按“待联系 → 已联系 → 已完成”单向流转，待联系或已联系可取消；已完成和已取消不能重新打开
- 用户已授权时，后台更新预约状态会发送订阅通知并可点击进入预约详情；发送失败不影响状态更新
- 后台会区分“提醒已发送”“用户未订阅”和“提醒发送失败”；真实故障可在错误日志按预约编号、目标状态或错误码查询
- 订阅通知中的更新时间固定按中国标准时间生成，不依赖云函数运行时所在时区
- 预约列表和详情页可直接拨打用户电话，详情页可复制手机号和 OpenID；拨号不会自动修改预约状态
- 用户取消与后台更新同时发生时不会互相覆盖，冲突操作会提示刷新后重试
- 预约 CSV 数据库查询必须使用导出字段白名单，不读取 OpenID 或未声明的未来字段；姓名、城市、备注等用户输入应保留原文并按文本导出，不能触发表格公式
- 预约 CSV 先在最多 2000 条预约中筛选和排序，再导出最多 500 条；超过范围时页面会明确提示分批导出
- 管理员可从“我的 → 存储清理”并行重试处理 `pending_file_deletions`，每次最多处理 5 条，并从最多 100 条候选中优先选择已到安全核验时间的任务，等待任务不得阻塞后续清理；结果需分别显示已清理、失败、等待核验、保留在用图片和无效任务；数据库按用途读取队列 ID、文件列表、重试次数及图片核验所需的来源、车辆上下文和最早处理时间，不加载历史错误或历史处理时间；真实失败会保留并累计重试次数，云存储确认文件已不存在时直接移除任务
- 仅管理员可进入“隐私申请处理”，首次载入时显示包含身份、说明和操作区的申请卡片骨架；申请状态只能按“待处理 → 处理中/已完成/未通过”单向流转
- 管理员可从隐私申请进入“相关数据清单”，只读核对当前用户的预约、收藏、匿名统计说明及其他隐私申请
- 相关数据清单和个人数据 CSV 的申请主记录、预约、收藏、其他隐私申请查询必须分别使用数据库字段白名单，不读取内部处理人、OpenID 副本或未声明字段
- 隐私申请管理列表与用户“我的申请”必须使用各自的数据库字段白名单；用户列表不得读取 OpenID、内部处理人或个人数据导出操作人
- 单个集合不可用或单类记录超过 200 条时，相关数据清单会标记为不完整；管理员不得据此直接完成查询、更正或删除申请
- 只有“查询信息”申请可在清单完整时导出个人数据 CSV；集合不可用或单类超过 200 条时必须禁止生成残缺文件
- 隐私数据 CSV 保留用户文本原文，并对 `=`、`+`、`-`、`@` 等表格公式前缀进行文本保护；导出审计只记录申请编号和分类条数，不记录目标 OpenID、手机号、申请正文或处理反馈
- 隐私数据 CSV 可在设备端打开、分享和二次确认后删除；只允许删除小程序自身目录内的 `.csv` 文件，重新导出成功后清理上一份不同文件
- 完整 CSV 生成成功后在对应申请中写入 `dataExportedAt` 和内部操作人 `dataExportedBy`；管理列表只返回生成时间，不返回操作管理员身份
- 相关数据清单首次载入显示申请上下文、核验进度、分类指标和记录分区结构化骨架；完成后显示预约、收藏和隐私申请三类核验覆盖进度、关联记录总数及各类完整/受限/不可用状态；预约记录以整行按压反馈和明确辅助名称进入详情，无关联记录时显示原生核验完成标识，申请上下文、敏感文件提示、导出结果与文件操作使用原生图标，不使用字符箭头或勾选代替
- “查询信息”申请未写入 `dataExportedAt` 时，前端不显示完成操作，服务端也必须拒绝将其更新为已完成；更正和删除申请不受此门槛影响
- 隐私申请管理卡片使用类型图标和已提交、处理中、已结束三阶段轨迹；账号、申请说明、处理反馈及数据交付状态分区清晰，核验与处理操作使用原生图标，无筛选结果时可恢复默认待处理队列
- 完成或驳回隐私申请时必须填写面向用户的处理说明；申请正文和处理说明不写入审计日志
- 审计日志可筛选 `privacyRequestDataInventory` 和 `privacyRequestDataExportCsv`，并查看核验或导出的分类数量与结果
- 审计日志按权限、车辆、预约、隐私和系统运营区分事件色彩及原生图标；中文动作标题、技术动作码、操作账号、发生时间和变更详情层级清晰，较长动作标题最多展示两行，无筛选结果时可一键重置
- 审计日志和错误日志可沿用当前筛选条件导出 CSV；加载更多与列表完成反馈使用对应原生图标；仅管理员可用，单次最多导出 500 条、最多扫描 2000 条
- 审计和错误日志 CSV 扫描必须使用各自的数据库字段白名单，禁止读取历史值快照、备注正文或服务端堆栈
- 日志 CSV 对 `=`、`+`、`-`、`@` 等表格公式前缀进行文本保护，错误日志不导出堆栈；导出审计不会记录搜索关键词
- 错误日志管理查询必须在数据库字段白名单阶段排除服务端堆栈；设备端只接收函数、阶段、记录标识、错误码和最多 300 字的消息摘要，完整堆栈需在受限云日志中排查
- 错误日志使用分组诊断时间线，中文服务名、技术函数名、发生时间、异常摘要、关联记录与排查建议层级清晰；查询、导出和文件操作使用原生图标，无筛选结果时可一键重置
- 错误日志列表和错误日志 CSV 必须在数据库字段白名单阶段排除历史 `openid`、`targetOpenid` 与服务端堆栈；页面搜索、摘要和导出表头均不得再提供身份字段
- 新建预约、我的预约详情、修改本人联系方式和取消预约的持久异常日志不得复制 OpenID、预约日期或服务端堆栈；仅保留预约/车辆编号、是否已登录和最多 300 字错误摘要
- 预约管理详情、状态更新、内部备注、协调安排、日历和 CSV 导出的持久异常日志不得复制管理员 OpenID 或服务端堆栈；管理员身份追踪统一由审计日志承担
- 车辆新增、编辑、状态、图片、停用、恢复和删除的持久异常日志不得复制管理员 OpenID、完整车牌、云文件地址或服务端堆栈；仅保留车辆编号、动作、认证状态和最多 300 字错误摘要
- 管理员初始化、权限保存、运营配置、匿名事件清理和存储清理队列的持久错误日志不得复制管理员 OpenID 或服务端堆栈；所有云函数运行时异常日志也不得直接打印任何 OpenID，只能记录是否已认证；上线测试必须自动扫描全部持久错误调用和 `console.error` / `console.warn` 对象
- 预约与日志 CSV 可从导出页面二次确认后删除；只允许删除小程序自身目录内的 `.csv` 文件，重新导出成功后会清理上一份不同文件
- 审计日志可筛选 `privacyRequestCreate` / `privacyRequestCancel` / `privacyRequestUpdateStatus`，并按申请 ID 搜索
- 审计日志可筛选 `bookingUpdateMyContact`，只记录变更字段名，不记录姓名、手机号、城市或备注原文
- 审计日志可筛选 `bookingUpdateCoordination`，只记录优先级和协调状态变化，不记录客户联系内容
- 审计日志可筛选 `analyticsCleanup`，只记录保留周期和处理数量，不记录具体事件内容
- 新增车辆图片只能来自 `vehicle-images/{当前车辆ID}/` 目录，不能跨车辆挂载云文件
- 新增或编辑车辆时“公开说明”和“内部备注”分开保存；敏感档案字段的修改日志只记录字段名，不记录修改前后的具体内容
- 编辑车辆后可真正清空城市、租赁参数、VIN、发动机号、公开说明和内部备注，重新进入编辑页确认旧值未残留
- 并发新增或修改为相同车牌时由唯一索引拦截，页面返回“车牌号已存在”
- 运营配置页加载失败时不会误保存默认值；品牌、首页、内容和预约四个分组使用对应原生图标，长文本显示实时字数，订阅模板显示启用状态，重试、恢复默认和保存使用对应原生图标，保存期间保存与恢复默认入口均禁用，任意编辑或恢复默认后吸底保存栏应提示存在未保存修改，成功读取或保存后恢复线上已同步状态
- 运营配置公开读取和预约状态通知查询 `app_configs` 时只能读取 `value`；配置保存前只能读取 `_id` 与 `value`，不得加载创建人、更新人或时间字段
- 车辆管理、预约管理、审计日志、错误日志页均有前置鉴权
- 所有使用后台权限守卫的页面必须在鉴权成功后才渲染管理内容；直接输入后台路径时不得短暂显示表单、日志或操作按钮
- 审计日志可筛到 `bookingCreate`、`bookingCancel`、`vehicleCreate`、`vehicleRetire`、`vehicleRestore`
- 预约创建审计只记录预约 ID 和车辆 ID，不重复保存车型、日期、城市、姓名、手机号或备注；车辆操作审计不重复保存完整车牌
- 审计日志查询必须在数据库字段白名单阶段排除备注正文和历史 `before/after` 字段值快照；页面只展示枚举、数量、字段名、协调状态变化和导出筛选摘要
- 运营配置和车辆编辑审计只记录发生变化的字段名，不重复保存配置内容、车辆资料或修改前后的具体值
- 错误日志可筛到 `vehicleUpdateStatus` 等关键函数

### 真机与审核

- Android 与 iPhone 各完成一次用户侧主流程
- 使用非管理员账号直接访问后台路由，确认无法读取或修改数据
- 客服、电话、分享、图片选择、图片上传与预览均正常
- 弱网、断网、空数据和云函数异常均有明确反馈
- 小程序名称、头像、简介、截图和车辆图片素材具有合法使用权

## 8. 上线后冒烟

- 创建一条预约，确认 `audit_logs` 出现 `bookingCreate`
- 取消该预约，确认 `audit_logs` 出现 `bookingCancel`
- 新增一辆车，确认 `audit_logs` 出现 `vehicleCreate`
- 停用并恢复车辆，确认 `audit_logs` 出现 `vehicleRetire` / `vehicleRestore`
- 人为制造一次后台异常，确认 `error_logs` 可被管理页查询

## 9. 风险提示

- 如果 `cloudbaserc.json` 未同步新增函数，部署时会漏掉 `bookingDetail`、`bookingMyDetail`、`errorLogList` 等能力
- 如果缺少 `audit_logs` / `error_logs` 集合，best-effort 日志会被忽略，主流程仍可运行，但线上排障和追溯会缺失
- 如果缺少 `pending_file_deletions` 集合，图片删除失败后无法进入重试队列，可能长期占用云存储容量
- 如果缺少 `privacy_requests` 集合，用户无法提交或查看个人信息申请，管理员处理页也无法加载
- 如果缺少 `favorites` 集合，车辆详情收藏状态和“我的收藏”页面无法使用
- 审计日志和错误日志管理页最多分批读取 2000 条；超过上限时页面会显示 `2000+` 提示，应使用 CSV 按类型、关键词或操作分批导出归档
- 车辆管理和预约管理最多分批读取 2000 条，超过上限时页面会提示缩小筛选范围；预约 CSV 最多扫描 2000 条、单次导出 500 条，避免生成过大的个人信息文件
- `roleList` 已支持分页，并可分批读取超过 200 条的角色记录；单次管理规模仍限制在 2000 条以内，超过该规模时应改用数据库游标分页

## 10. 建议上线顺序

1. 部署全部云函数
2. 创建集合并补索引
3. 配置 `BOOTSTRAP_TOKEN`
4. 配置数据库、云存储安全规则和微信隐私保护指引
5. 真机跑用户侧主流程
6. 真机跑管理侧主流程
7. 检查审计日志与错误日志是否可见
8. 上传体验版并按审核路径复测
9. 再提交正式审核
